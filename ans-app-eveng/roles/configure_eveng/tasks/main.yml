---
- name: Install gdown using pip
  pip:
    name: gdown
    state: present
    executable: pip3

# to heterogenous config to download images from multiple sources  in different format
# - name: Check if platform-version is being already installed in eve-ng
#   eveng_fetch:
#     host: "{{ inventory_hostname }}"
#     username: "{{ eveng_username }}"
#     password: "{{ eveng_admin_pass }}"
#     nodes: "{{ eveng.nodes }}"
#   register: out_images_presence
#   no_log: true
#   delegate_to: localhost

# - name: Check if platform-version is supported
#   ansible.builtin.assert:
#     that: "{{ download_links[node['platform_version']] is defined }}"
#   loop: "{{ out_images_presence.nodes }}"
#   loop_control:
#     label: "{{ node.platform_version }}"
#     loop_var: node

# - name: Install missing images
#   ansible.builtin.include_tasks: "platforms/{{ missing_image.split('-')[0] }}/install.yml"
#   loop: "{{ out_images_presence.nodes | selectattr('firmware_present', 'equalto', false) | map(attribute='platform_version') | unique }}"
#   loop_control:
#     loop_var: missing_image

- name: Copy tooling gdown script
  ansible.builtin.copy:
    src: ../scripts/gdown.py
    dest: /opt/unetlab/addons/qemu/
    mode: '0755'

- name: Set networking
  ansible.builtin.include_tasks: "set_networking.yml"
